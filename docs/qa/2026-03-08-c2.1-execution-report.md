# C2.1 técnico — ejecución real D1→D4 (2026-03-08)

## Skills usadas
- `coding-agent`: para orquestar refactor técnico y validaciones de build/tests.
- `ui-design`: para preservar consistencia visual/UX al separar bloques UI (sin cambios funcionales).

---

## D1 — Baseline + diseño interno (sin tocar comportamiento)

### Baseline funcional congelado
Escenarios esperados:
1. Búsqueda normal con resultados.
2. Sin resultados (`empty`) con acciones de relajación.
3. Error backend (`error`) con retry.
4. Rate-limit (`rate`) con countdown.
5. Flujo de filtros strict/soft y flex days.

### Medición inicial
- `frontend/src/app/(private)/quick-search/page.tsx`: **4176 líneas** (antes de C2).
- Responsabilidades mezcladas detectadas:
  - estado UI + refs,
  - validaciones,
  - construcción de request,
  - normalización de response,
  - rendering de UI grande,
  - telemetría/eventos.

### Estructura objetivo (aprobada y aplicada)
- `frontend/src/modules/quick-search/types.ts`
- `frontend/src/modules/quick-search/state/useQuickSearchController.ts`
- `frontend/src/modules/quick-search/api/buildQuickSearchRequest.ts`
- `frontend/src/modules/quick-search/api/normalizeQuickSearchResponse.ts`
- `frontend/src/modules/quick-search/components/*`
- `frontend/src/app/(private)/quick-search/page.tsx` (orquestador fino)

---

## D2 — Extracción de lógica (estado + API mapping)

### Ejecutado
1. Tipos de dominio extraídos (`types.ts`).
2. Request builder aislado (`api/buildQuickSearchRequest.ts`).
3. Response normalizer aislado (`api/normalizeQuickSearchResponse.ts`).
4. Controlador principal de estado aislado (`state/useQuickSearchController.ts`).

### Compatibilidad
- Contrato `/api/v1/search/quick` preservado (query shape sin cambios funcionales).
- Wrappers de compatibilidad temporal mantenidos:
  - `requestBuilder.ts`
  - `responseNormalizer.ts`
  - `useQuickSearchMainState.ts`

---

## D3 — Extracción UI por bloques

### Ejecutado
Componentes creados/separados:
- `QuickSearchSearchForm`
- `QuickSearchAdvancedFilters`
- `QuickSearchLoadingProgress`
- `QuickSearchResultsList`
- `QuickSearchStatePanels`

`page.tsx` queda como wrapper cliente que monta `QuickSearchView`.

### Estado funcional esperado
- Estados `loading / empty / error / success` mantenidos.
- Navegación y acciones de watchlist/deeplink mantenidas.

---

## D4 — QA + hard checks + cierre

### Build/tests (evidencia)
Comandos:
```bash
npm run build
npm run test -- tests/quick-search-utils.test.ts tests/quick-search-copy.test.ts tests/quick-search-refactor-utils.test.ts
```

Resultado:
- Build: ✅ PASS
- Tests quick-search: ✅ PASS (10/10)

### Métrica antes/después (líneas)
- Antes: `app/(private)/quick-search/page.tsx` = **4176**
- Después: `app/(private)/quick-search/page.tsx` = **7**

### Bugs encontrados y fix aplicados en C2.1
1. **Nomenclatura/estructura no alineada** con plan C2.1:
   - Solución: mover a `api/*` y `state/*` + wrappers de compatibilidad.
2. **Copy inconsistente en UI** (`Reset todo` hardcoded):
   - Solución: nueva clave i18n `resetAll` (ES/EN) y uso en botones.

### Capturas D4 (completadas)
Escenarios generados y guardados en `docs/qa/screenshots/c2.1/`:
1. `01-busqueda-normal.png`
2. `02-sin-resultados.png`
3. `03-error-backend.png`
4. `04-strict-soft.png`

Automatizado con Playwright (`frontend/scripts/c2_1_quicksearch_screenshots.mjs`) con stubs de API para reproducibilidad.

---

## Estado de checks C2.1
- D1 baseline documentado: ✅
- D2 extracción lógica completada: ✅
- D3 composición modular aplicada: ✅
- D4 build/tests + evidencias técnicas: ✅
- Capturas visuales manuales: 🟡 pendiente de ejecución manual guiada
